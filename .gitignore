# Reference clone of OpenKyrozen (analysis only, do not commit)
.openkyrozen_ref/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Storage & logs
chroma_memory/
/logs/
projects/
*.log
*.db
kyrozen_tasks.json

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Environment / secrets
.env
*.env
*.kyrozen_config.json
.kyrozen_config.json
.kyrozen_jwks_cache.json
.secrets/

# Agent tooling (WorkBuddy local memory, not for the repo)
.workbuddy/

# Vite TypeScript config temp artifacts
*.timestamp-*.mjs

# Local-only design & planning documents (kept on disk, never pushed to GitHub).
# 项目方案 / 设计 / 架构 / 阶段报告 / 测试报告等内部文档统一放这里，仅本地保留。
docs/
# 但保留 docs/ 下的部署说明文档（运维类，非设计文档，公开仓库需要）
!docs/CLOUDFLARE_TUNNEL.md
!docs/PRODUCTION_SERVER.md
!docs/SELFHOSTED_DEPLOYMENT.md
